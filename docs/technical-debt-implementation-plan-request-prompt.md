IMPLEMENTATION PLAN REQUEST PROMPT (bacs-file-data-generator)

Objective:
Create a phased, test-first implementation plan to remediate technical debt in the `bacs-file-data-generator` package. Align with the technical debt report (2025-11-30) and the following emphases:
Note on approach: This is a big-bang change, not a strangler pattern. The plan should deliver comprehensive removals and replacements in a single coordinated set of changes, with clear rollback notes if needed.
Communication stance: Treat the package as a greenfield HTTP data generator for any user-facing docs and API descriptions. Do not include migration hints or references. The implementing agent should still consider migration context internally to guide decisions and risk mitigation.

1. Remove deprecated paths: CLI entry (`src/cli/generate.ts`) and any Express.js HTTP server code (Express is deprecated). Replace HTTP with Fastify inside this package.
2. After removal, reorganize unit tests so each test file sits beside its System Under Test (SUT). Eliminate central `tests/` duplicates and legacy/obsolete specs (e.g., skipped validator/factory tests).
3. Wire SmartDebit (SDDirect) and `bacs18PaymentLines` strategies fully into the factory so multi-file-type support is complete (Strategy Pattern).
4. Enforce single responsibility: the package must ONLY generate data and serve it via an HTTP API. Supported output formats: CSV, Bacs-standard CSV variants, or JSON. No orchestration, job scheduling, or external side-effect management beyond returning the generated payload in the HTTP response.
5. Purge committed output artifacts and any files generated by previous test runs (output, `gitbacsbacs-file-data-generatoroutputtest-cli-smoke/`, stray golden CSVs) — replace with deterministic, on-demand test generation under temp paths.
6. Hard requirements: determinism (seeded RNG + optional fixed timestamp), structured error model (AppError + codes), ≥90% unit test coverage, removal of unused dependency `lts`, consolidation of seeding logic (`ensureSeeded()` single entry).

Context Constraints:

- Treat `CuddleYourCat.code-notes` and any top-level tools/resources folders as reference-only (read-only).
- The package provides an HTTP API (use Fastify). Keep the API surface minimal and focused on data generation endpoints.
- Preserve existing public API for data generation where reasonable; justify any breaking change.
- Avoid adding responsibilities beyond generating data and serving it over HTTP.

Current Issues to Address (Map to TD codes):

- TD-002: Test locality & duplication.
- TD-003/014/016: Determinism (RNG, timestamps, single seeding).
- TD-005/017: Error model consistency (replace raw Error).
- TD-001/010/019: Factory gaps (add SDDirect + Bacs18 strategies).
- TD-006: Oversized EaziPay generator (modularization).
- TD-013: Remove unused dependency.
- Output artifact cleanup.
- Deprecated interfaces: remove CLI and Express; introduce Fastify HTTP.

Deliverables the Plan Must Produce:

1. Phased roadmap (Phase 0–6) with explicit goals:
   - Phase 0: Inventory & safety net (baseline coverage snapshot; enumerate deprecated assets).
   - Phase 1: Remove CLI and Express, delete related scripts; introduce Fastify server with minimal routes (`POST /generate` or similar).
   - Phase 2: Test relocation beside SUT; delete obsolete/duplicate central tests; enforce coverage gates (Vitest).
   - Phase 3: Determinism layer (DeterminismContext: clock + rng injection, fixed timestamp env hook).
   - Phase 4: Factory strategy expansion (handlers map for EaziPay, SDDirect, Bacs18PaymentLines) with unit tests per handler.
   - Phase 5: EaziPay generator refactor (split `validation.ts`, `rowBuilder.ts`, `formatting.ts`, `seeding.ts`) + error model unification with `AppError`.
   - Phase 6: Artifact purge & CI adjustments (ensure repo is clean after tests; add `.gitignore` rules).
2. For each phase: entry criteria, exit criteria, tasks, risks, mitigation, rollback notes.
3. HTTP API design:
   - Fastify setup (no Express). Minimal routes:
     - `POST /generate` with body `{ fileType, rows, format, fakerSeed?, fixedTimestamp? }`.
     - Response returns generated payload (CSV string or JSON) and metadata.
   - Validation middleware aligned with validators in `src/validators`.
   - Strict input validation; deterministic seed handling; error model mapping to HTTP status codes.
4. Explicit test strategy:
   - Co-located unit tests (`*.test.ts`) per module.
   - HTTP route tests using Fastify’s inject; deterministic outputs via seed + fixed timestamp.
   - Coverage target ≥90% overall; critical modules >95% where feasible.
5. Design patterns justification:
   - Strategy Pattern for factory mapping file types to generators.
6. Determinism design:
   - Injected RNG function (seeded LCG or library).
   - Clock abstraction (default `Date.now()` vs `FIXED_TIMESTAMP` env in tests).
   - Single seeding gate (`ensureSeeded()`).
7. Error model spec:
   - Error codes: `UNSUPPORTED_FILE_TYPE`, `INVALID_SEED`, `VALIDATION_FAILED`.
   - Use `AppError` consistently; map to HTTP statuses in Fastify error handler.
8. Refactor boundaries:
   - Replace “god file” `generator.ts` with `validation.ts`, `rowBuilder.ts`, `formatting.ts`, `seeding.ts`.
9. Dependency audit/removal plan:
   - Remove `lts`, confirm no transitive usage; update lockfile.
10. File/Path cleanup plan:

- List all output artifact directories to delete.
- Update `.gitignore` to exclude ephemeral outputs.

11. Acceptance Criteria Summary:

- CLI and Express removed; Fastify HTTP API in place.
- No stray output artifacts tracked by git.
- Factory `handlers` map includes EaziPay, SDDirect, Bacs18PaymentLines with passing tests.
- Deterministic test runs reproducible via seed + timestamp.
- Coverage ≥90% and CI gate enforced.
- EaziPay generator modular; no file >300 lines.
- Lint + typecheck clean; no unused dependencies.

12. Risks & Mitigations:

- Risk: Removing CLI breaks external scripts → Provide migration doc to use HTTP API.
- Risk: Determinism layer may affect performance → Add micro-benchmark or note.
- Risk: Refactor may alter formatting subtly → Golden comparison tests before/after.

Formatting & Output Expectations:

- Provide the plan in structured Markdown with sections and concise bullets.
- Include a final “Next Actions” quick list.
- Planning only; do not write code.

Agent Working Instructions:

- Enumerate deprecated assets (paths) and confirm no remaining imports after removal.
- Implement Fastify HTTP plan while keeping responsibilities minimal (data generation + response only).
- Provide ESLint/Prettier commands and Vitest coverage config recommendations.
- Order tasks to minimize merge conflicts: remove deprecated code early, then add Fastify.

Assumptions (state explicitly in plan):

- Package will expose only an HTTP API for generation and reusable library functions as needed.
- External consumers migrate from CLI to HTTP API.
- Environment provides `FAKER_SEED` and optionally `FIXED_TIMESTAMP`.

Acceptance Gate Reminders:

- ESLint passes.
- Prettier passes or is auto-applied.
- Vitest coverage threshold enforced.
- AppError used for specified error cases.
- Repo clean of output artifacts post-test.

Produce:
A single comprehensive implementation plan meeting all the above. Ask for clarification ONLY if a blocking ambiguity remains (e.g., missing SmartDebit details).

Agent-only Internal Checklist (do not include in user-facing docs):

- Deprecated surfaces to remove: `src/cli/generate.ts` (CLI), any Express-based server files under `src/server` or `src/http` using Express, legacy integration harnesses referencing CLI/Express, and related npm scripts.
- Replace HTTP with Fastify: minimal `POST /generate` route; use Fastify inject for tests.
- Factory wiring: ensure `handlers` include `EaziPay`, `SDDirect` (SmartDebit), and `Bacs18PaymentLines` with validators.
- Determinism: centralized `ensureSeeded()`; injected RNG; optional `FIXED_TIMESTAMP` for tests.
- Error model: replace raw `Error` with `AppError`; add codes `UNSUPPORTED_FILE_TYPE`, `INVALID_SEED`, `VALIDATION_FAILED`.
- Test locality: move tests next to SUT; delete central duplicates and skipped suites.
- Artifact purge: delete tracked outputs (`output/`, smoke CSVs); update `.gitignore` to exclude ephemeral outputs.
- Dependency audit: remove `lts`; run lint, format, typecheck; enforce coverage ≥90%.
