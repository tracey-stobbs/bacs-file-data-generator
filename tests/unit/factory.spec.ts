// Legacy factory tests removed after narrowing scope to EaziPay only.
// Kept as a skipped suite so test file globs remain stable.
import { describe } from 'vitest';

describe.skip('legacy factory tests (removed with SDDirect & Bacs18)', () => {
    /* Intentionally left blank. */
});