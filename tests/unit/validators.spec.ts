// Legacy validator tests removed (fieldValidators module no longer exists after scope reduction).
import { describe } from 'vitest';

describe.skip('legacy field validator tests (removed)', () => {
    /* Intentionally empty. */
});